[package]
name = "wgpu-terminal"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
pollster = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
wgpu = { version = "30.0", features = ["trace"] }
